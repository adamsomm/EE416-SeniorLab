#pragma once

// A simple structure to hold 2D coordinates
struct Point {
    double x;
    double y;
};

// A simple structure to define the room boundaries
struct Rect {
    double min_x;
    double max_x;
    double min_y;
    double max_y;
};
